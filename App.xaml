<Application x:Class="Timestamp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Timestamp"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="FontBaseStyleLabel" TargetType="{x:Type Label}">
                <Setter Property="Foreground" Value="#FF6C6C6C"/>
            </Style>
            <Style x:Key="FontBaseStyleTextBlock" TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="#FF6C6C6C"/>
            </Style>
            <Style x:Key="ButtonBaseStyle" TargetType="{x:Type Button}">
                <Setter Property="Background" Value="White"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Padding" Value="10,5,10,5"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Foreground" Value="#FF6C6C6C"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>

</Application>
